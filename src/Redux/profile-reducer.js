let initialState = {
    id: 1,
    name: 'Кирилл',
    surname: 'Гекман',
    secondName: 'Эдупдович',
    age: 99,
    isMan: true
}

const profileReducer = (state = initialState, action) => {
    return state
}

export default profileReducer;